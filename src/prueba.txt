TRAEME TODO DE_LA_TABLA usuarios DONDE edad > 18; 
TRAEME LOS_DISTINTOS nombre DE_LA_TABLA clientes DONDE ciudad ='Madrid';
METE_EN usuarios (nombre, edad) LOS_VALORES ('Juan', 25); 
ACTUALIZA empleados SETEA salario = 3000 DONDE puesto = 'ingeniero'; 
CAMBIA_LA_TABLA empleados ELIMINA_LA_COLUMNA direccion; 
BORRA_DE_LA_TABLA clientes DONDE edad ENTRE 18 Y 25; 
CAMBIA_LA_TABLA empleados AGREGA_LA_COLUMNA direccion VARCHAR(255) NO_NULO; 
TRAEME TODO DE_LA_TABLA pedidos MEZCLANDO clientes EN pedidos.cliente_id = clientes.id DONDE clientes.ciudad = 'Barcelona'; 

TRAEME CONTANDO(TODO) DE_LA_TABLA ventas AGRUPANDO_POR producto WHERE_DEL_GROUP_BY COUNT(TODO) > 5; -> Funciona miti

'''
    
 def traducir_usql_a_sql(consulta_usql):
    lexer.input(consulta_usql)
    try:
        while True:
            tok = lexer.token()
            if not tok:
                break
            print(tok)  
        resultado = parser.parse(consulta_usql)
        return resultado
    except Exception as e:
        print(f'Error al procesar la consulta USQL: {str(e)}')
        return None
 '''